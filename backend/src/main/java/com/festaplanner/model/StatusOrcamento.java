package com.festaplanner.model;

/** Ciclo de vida do orçamento, espelhando a coluna STATUS da tela de Pedidos do ADM. */
public enum StatusOrcamento {
    RASCUNHO,
    NOVO,
    PENDENTE,
    PRE_RESERVA,
    CONFIRMADO,
    RECUSADO
}
